// Engine 4: Realtime Relay — FUTURE
// Scaffold only. Implementation pending when WebSocket/SSE support is added.
//
// Planned routes:
//   WS  /ws   — WebSocket relay (NATS events → browser, L1 filtered)
//   GET /sse  — Server-Sent Events relay
//
// Will use @fastify/websocket or native SSE with NATS JetStream consumer.

export {}; // module marker
