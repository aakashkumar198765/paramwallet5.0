/**
 * Stub: create a document via ParamGateway pipeline.
 * Returns { success: true } — no real HTTP call yet.
 */
export async function createDocument(
  _pipelineId: string,
  _payload: unknown
): Promise<{ success: true }> {
  return { success: true };
}
